apiVersion: v1
kind: Service
metadata:
  name: compliant-app
  namespace: compliant-app
  labels:
    app: compliant-app
    environment: demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: compliant-app
